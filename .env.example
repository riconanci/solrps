# .env.example - Phase 2 Configuration Template

# Database (unchanged from Phase 1)

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/solrps?schema=public"
TREASURY_ADDRESS="TREASURY_MOCK"
BURN_ADDRESS="BURN_MOCK"
REVEAL_DEADLINE_SECONDS=600

# Phase 2 Feature Flags - START HERE FOR MIGRATION
NEXT_PUBLIC_ENABLE_PHASE2=true          # Enable Phase 2 UI features
NEXT_PUBLIC_USE_BLOCKCHAIN=true       # Start with false, switch to true when ready to use real wallets

# Solana Configuration (required when USE_BLOCKCHAIN=true)
NEXT_PUBLIC_SOLANA_CLUSTER=devnet
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com

# Optional: Custom RPC endpoints for better performance
# NEXT_PUBLIC_SOLANA_RPC_URL=https://rpc.ankr.com/solana_devnet
# NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com

# Future Phase 2 Configuration (not needed yet)
# NEXT_PUBLIC_TOKEN_MINT_ADDRESS=          # SPL token mint address (future)
# NEXT_PUBLIC_TREASURY_ADDRESS=            # Real treasury wallet address (future)
# NEXT_PUBLIC_BURN_ADDRESS=                # Real burn wallet address (future)  
# NEXT_PUBLIC_PROGRAM_ID=                  # Anchor program ID (future)

# Development Settings
NODE_ENV=development

# === PHASE 2 MIGRATION GUIDE ===
#
# Step 1: Enable Phase 2 UI
# Set NEXT_PUBLIC_ENABLE_PHASE2=true
# This shows Phase 2 UI elements but still uses mock wallets
#
# Step 2: Test Real Wallets (Development)
# Set NEXT_PUBLIC_USE_BLOCKCHAIN=true
# Install Phantom/Solflare browser extension
# Test wallet connection flow
#
# Step 3: Production Deployment
# Keep USE_BLOCKCHAIN=true for production
# Set proper SOLANA_CLUSTER=mainnet-beta for production
# Update RPC URL to production endpoint
#
# === TESTING CONFIGURATION ===
#
# For local development testing:
# - ENABLE_PHASE2=true, USE_BLOCKCHAIN=false (Mock wallets)
# - ENABLE_PHASE2=true, USE_BLOCKCHAIN=true (Real wallets)
#
# For production:
# - ENABLE_PHASE2=true, USE_BLOCKCHAIN=true
# - SOLANA_CLUSTER=mainnet-beta
# - Use production RPC endpoint